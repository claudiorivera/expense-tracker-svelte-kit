import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export {
	type ButtonProps as Props,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	Root,
	Root as Button,
};
